{
    "name": "rpi-wifi-connection",
    "license": "MIT",
    "scripts": {
        "build": "tsup ./src",
        "build:dts": "tsc --emitDeclarationOnly",
        "build:all": "npm run build && npm run build:dts"
    },
    "devDependencies": {
        "@types/node": "^20.11.5",
        "tsup": "^8.2.4",
        "typescript": "^5.3.3"
    }
}
